#include <stdio.h>

int main()
{
	int a,b,c;
	
	printf("이차 방정식의 계수 a,b,c를 입력하시오");
	scanf("%d %d %d",&a, &b, &c);
	
	if(a=0)
     printf("%d/%d", c,d)
    
    else
    printf("((-b+-(sqrt(b*b-4*a*c))/(2*a))");
    
    return 0;
    
	
}
